import React from 'react';

const ProfileLayout = ({ children }) => {
    return (
        <>
            <h1>Profile Layout</h1>

            {
                children
            }
        </>
    );
};

export default ProfileLayout;